# Cấu hình API Key của Google Gemini
# Đổi tên tệp này thành .env và điền mã khoá của bạn phía sau dấu bằng.
# KHÔNG commit tệp .env thật lên Git để tránh rò rỉ API Key.

GEMINI_API_KEY=your_key_here
PORT=3000
